apiVersion: 1

# list of datasources to insert/update depending
# what's available in the database
datasources:
- name: financial
  type: postgres
  access: proxy
  url: ${DATABASE_URL}
  jsonData:
    sslmode: require
  editable: true
