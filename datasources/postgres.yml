apiVersion: 1

# list of datasources to insert/update depending
# what's available in the database
datasources:
- name: financial
  type: postgres
  access: proxy
  url: ${DATABASE}
  jsonData:
    sslmode: require
  editable: false
